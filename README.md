
Crack Analyzer — Render-ready
============================

Minimal Streamlit app for crack detection. Designed to be uploaded to GitHub and deployed on Render.

Start command (Render):
streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
